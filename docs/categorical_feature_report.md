# Categorical Feature Encoding Audit

Generated by `check_categorical_features.py`. Thresholds: one-hot up to 15 categories, frequency/target encoding up to 50, anything higher is flagged as a likely identifier.

## combined_sample.parquet (WWT)

Shape: 204,196 rows x 221 columns

**Already excluded (ID/label columns, not encoding candidates):** pkt_src_ip, pkt_dst_ip, pkt_device_mac, pkt_eth_src_oui, pkt_eth_dst_oui, pkt_label, flow_Flow ID, flow_Src IP, flow_Dst IP, flow_Timestamp, flow_Label

| Column | Unique values | Top value | Top value % | Recommendation |
|---|---|---|---|---|
| `pkt_src_mac` | 79 | `3c:18:a0:41:c3:a0` | 40.7% | HIGH CARDINALITY (79 unique / 204196 rows = 0.0%). Likely an identifier that should be dropped, not encoded. If genuinely a category (not an ID), use frequency/target encoding, never one-hot. |
| `pkt_dst_mac` | 78 | `3c:18:a0:41:c3:a0` | 46.0% | HIGH CARDINALITY (78 unique / 204196 rows = 0.0%). Likely an identifier that should be dropped, not encoded. If genuinely a category (not an ID), use frequency/target encoding, never one-hot. |
| `pkt_handshake_version` | 3 | `-1` | 99.4% | One-hot encode -- 3 categories is manageable (3 new columns). |
| `pkt_tls_server` | 46 | `none` | 99.8% | One-hot NOT recommended (46 categories -> 46 new columns). Consider frequency encoding or target encoding instead. |
| `pkt_http_request_method` | 4 | `none` | 99.6% | One-hot encode -- 4 categories is manageable (4 new columns). |
| `pkt_http_host` | 1 | `none` | 100.0% | Constant column -- only 1 distinct value across all rows. Provides no signal; consider dropping. |
| `pkt_user_agent` | 40 | `none` | 99.6% | One-hot NOT recommended (40 categories -> 40 new columns). Consider frequency encoding or target encoding instead. |
| `pkt_dns_server` | 179 | `none` | 94.8% | HIGH CARDINALITY (179 unique / 204196 rows = 0.1%). Likely an identifier that should be dropped, not encoded. If genuinely a category (not an ID), use frequency/target encoding, never one-hot. |
| `pkt_highest_layer` | 30 | `TCP` | 61.1% | One-hot NOT recommended (30 categories -> 30 new columns). Consider frequency encoding or target encoding instead. |
| `pkt_http_uri` | 75 | `none` | 99.3% | HIGH CARDINALITY (75 unique / 204196 rows = 0.0%). Likely an identifier that should be dropped, not encoded. If genuinely a category (not an ID), use frequency/target encoding, never one-hot. |
| `pkt_http_content_type` | 9 | `none` | 99.7% | One-hot encode -- 9 categories is manageable (9 new columns). |

## flow_training_sample.parquet

Shape: 204,196 rows x 85 columns

**Already excluded (ID/label columns, not encoding candidates):** label

| Column | Unique values | Top value | Top value % | Recommendation |
|---|---|---|---|---|
| `Flow ID` | 204,193 | `192.168.137.16-192.168.137.1-53302-53-17` | 0.0% | HIGH CARDINALITY (204193 unique / 204196 rows = 100.0%). Likely an identifier that should be dropped, not encoded. If genuinely a category (not an ID), use frequency/target encoding, never one-hot. |
| `Src IP` | 331 | `192.168.137.41` | 20.9% | HIGH CARDINALITY (331 unique / 204196 rows = 0.2%). Likely an identifier that should be dropped, not encoded. If genuinely a category (not an ID), use frequency/target encoding, never one-hot. |
| `Dst IP` | 1,803 | `192.168.137.1` | 34.9% | HIGH CARDINALITY (1803 unique / 204196 rows = 0.9%). Likely an identifier that should be dropped, not encoded. If genuinely a category (not an ID), use frequency/target encoding, never one-hot. |
| `Timestamp` | 65,089 | `07/10/2022 03:54:33 PM` | 0.0% | HIGH CARDINALITY (65089 unique / 204196 rows = 31.9%). Likely an identifier that should be dropped, not encoded. If genuinely a category (not an ID), use frequency/target encoding, never one-hot. |
| `Label` | 1 | `NeedManualLabel` | 100.0% | Constant column -- only 1 distinct value across all rows. Provides no signal; consider dropping. |
